from pathlib import Path

DEFAULT_DIR = Path("ui 26.11.2025")

class PlayerPortrait:
    default_file = DEFAULT_DIR / "HUD/hud_characterDetails.rml"
    destination_file = "/ui/HUD/hud_characterDetails.rml"

class Minimap:
    default_file = DEFAULT_DIR / "HUD/hud_Minimap.rml"
    destination_file = "/ui/HUD/hud_Minimap.rml"

class Taskbar:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarCenter.rml"
    destination_file = "/ui/HUD/hud_taskBarCenter.rml"

class WeaponsHud:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
    destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class CombatArtsHud:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
    destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class TaskbarInventory:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarCenter.rml"
    destination_file = "/ui/HUD/hud_taskBarCenter.rml"

class TaskbarCA:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarCenter.rml"
    destination_file = "/ui/HUD/hud_taskBarCenter.rml"

class WeaponsInventory:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
    destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class CombatArtsInventory:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
    destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class WeaponsCA:
        default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
        destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class CombatArtsCA:
    default_file = DEFAULT_DIR / "HUD/hud_taskBarActionSlots.rml"
    destination_file = "/ui/HUD/hud_taskBarActionSlots.rml"

class Tabmap:
    default_file = DEFAULT_DIR / "HUD/hud_Tabmap.rml"
    destination_file = "/ui/HUD/hud_Tabmap.rml"

class AltText:
    default_file = DEFAULT_DIR / "HUD/hud_AltText.rml"
    destination_file = "/ui/HUD/hud_AltText.rml"

class FloatingText:
    default_file = DEFAULT_DIR / "HUD/hud_FloatingText.rml"
    destination_file = "/ui/HUD/hud_FloatingText.rml"

class ItemsTooltip:
    default_file = DEFAULT_DIR / "Ingame/items_Tooltip.rml"
    destination_file = "/ui/Ingame/items_Tooltip.rml"